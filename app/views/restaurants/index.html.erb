<h1>Best restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %> (<%= restaurant.rating %>/5)</li>
  <% end %>
</ul>

<%= link_to "Add a new restaurant", new_restaurant_path, :class => "btn btn-primary" %>
