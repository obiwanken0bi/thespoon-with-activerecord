<div class="form">
  <%= simple_form_for(restaurant) do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :rating %>
    <%= f.submit %>
  <% end %>
</div>

<%= link_to "Back to the list of restaurants", restaurants_path %>
